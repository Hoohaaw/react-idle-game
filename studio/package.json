{
  "name": "the-idle-game-studio",
  "private": true,
  "version": "1.0.0",
  "description": "Sanity Studio — authored game content (character defs + bespoke blessing trees) for The Idle Game",
  "type": "module",
  "scripts": {
    "dev": "sanity dev",
    "start": "sanity start",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "schema:deploy": "sanity schema deploy",
    "typegen": "sanity schema extract && sanity typegen generate"
  },
  "dependencies": {
    "@sanity/icons": "^3",
    "@sanity/vision": "^3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sanity": "^3",
    "styled-components": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
