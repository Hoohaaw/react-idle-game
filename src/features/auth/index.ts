// Public API of the Auth feature.
// Import from '@/features/auth' — never reach into the feature's internals (./components/*).

export { AuthPage } from './AuthPage'
export { RequireAuth } from './RequireAuth'
